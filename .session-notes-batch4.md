# Batch-4 GAP reconciliation — working notes (Jul 18-19, 2026)

Remaining open todo items (todo.md ~line 266-273):
- [x] GAP-O pin-drop/prospective (done, 2 specs pass)
- [x] GAP-J dimensional receipts (done, sites.dimensionReceipts + RefineChips card, 4 specs pass)
- [x] GAP-K account deletion (done, account.deleteAllData + deleteAllUserData + Account.tsx DeleteAccountCard, 2 specs pass)
- [x] GAP-L site roles (done, siteRoleOf/assertSiteViewer/assertSiteActor in dbHelpers; sites.get returns myRole; sites.sharedWithMe; sites.members.{list,add,remove}; refine actor-gated; updateSite widened to actor; listInsights+getLatestAnalysis widened to viewer; Sites.tsx SharePanel + SharedWithMe; 5 specs pass in server/siteRoles.test.ts)
- [ ] GAP-N Portfolio Manager export + portfolio verified-savings edition — IN PROGRESS:
  - server/reports.ts: added PortfolioExportRow, ESPM_TYPE map, portfolioManagerCsv(), PortfolioVerifiedData, assemblePortfolioVerified() — DONE
  - TODO next: add reports.portfolioExport (Pro, builds rows from sites.portfolio-like rollup or listSites+summary insight metrics) + reports.portfolioVerified procedures in server/routers.ts reports router (~line 2526); Pro-gated via requireTier(tierOf(ctx.user),"pro",...); REPORT_FEATURE_NAME map at line ~43 may need new keys or reuse.
  - Data for export rows: per-site summary insight kind==="summary" metrics.currentCost.total, annualUsageKwh from metrics? Portfolio query at routers.ts line 1157 computes rollups from h.getLatestAnalysis + meters; simplest: reuse same pattern or extract. euiBasis from baseline confidenceLabel. verifiedSavings per site: h.listMeasureImplementations sum, or h.totalVerifiedSavings(ctx.user.id) for total.
  - Client: Portfolio.tsx (client/src/pages/console/Portfolio.tsx) add "Export for Portfolio Manager (CSV)" button downloading portfolioExport CSV + a "Portfolio verified-savings statement" view/print maybe in Reports.tsx.
  - Tests: server/portfolioExport.test.ts — assert ESPM header row, Other-mapping for unknown type, blank (not fabricated) fields for unanalyzed site, disclaimer row, verified total only counts persisted impl verdicts.
- [x] GAP-Q DONE (deriveUtilityTriple + STATE_GAS_UTILITY in server/cascade.ts; quickCreate returns utilityTriple; sites.utilityReveal query; QuickStart revealUtilities() toast in both create paths; 3 specs pass in server/utilityReveal.test.ts)
- [ ] GAP-R EN/ES parity CI test + broader string coverage — IN PROGRESS:
  - i18n at client/src/lib/i18n.ts: EN const (~24 keys) + ES Partial. t() falls back to EN. Used only in client/src/pages/Home.tsx (nav/hero/estimator/footer keys). Hero/sub only use t() when lang==='es' (EN hero is different copy — intentional).
  - Plan: (1) add new keys for how-it-works heading/sub + tiers heading + tier persona lines? Home.tsx section headings at lines ~229-253 are hardcoded EN ("One pipeline, actual or hypothetical.", "Tiers"). Add keys howit.title/howit.sub/tiers.title with ES translations and wire into Home.tsx (keep EN copy identical to current text).
  - (2) CI parity test: client-side vitest? Vitest config covers server/*.test.ts — check vitest.config.ts include pattern; can import client/src/lib/i18n.ts from a server-side test file (no DOM needed except localStorage guard — t(key,lang) is pure). Write server/i18nParity.test.ts asserting: every ES key exists in EN (no orphans), ES covers 100% of a REQUIRED_PUBLIC_FUNNEL list of keys, no ES value equals its EN value (untranslated copy-paste), no empty strings.
- [ ] GAP-S Winter-sewer story card client-side — not started. Server likely has winter-average water/sewer logic — grep server for "sewer" / "winter" insight kind; task = render a story card client-side (probably in Dashboard/insight feed) from that insight.

Batch state: tsc clean, all specs green so far (pinDrop 2, dimensionReceipts 4, accountDeletion 2, siteRoles 5, portfolioExport 6, utilityReveal 3). NO checkpoint saved yet this batch. Stale console error 'tinyint is not defined' persists in devserver log — restart server before screenshots/checkpoint.

Key facts:
- routers.ts ~2900 lines; reports router at ~2526; account router ~2361; sites router ~266.
- Test pattern: ctxFor/makeUser inline per test file (see server/siteRoles.test.ts).
- tsc clean as of GAP-L. Dev-server "tinyint is not defined" console error is STALE (from before schema import fix; typecheck passes; may need webdev_restart_server before screenshots).
- No checkpoint saved yet this batch — save after GAP-N or at delivery.
- i18n: GAP-R mentions EN/ES parity — check client/src/lib or i18n module for string tables (not yet inspected).
- GAP-Q: quickCreate cascade derives utilityName (electric); reveal moment = show electric+gas+water providers derived from one address in QuickStart result. Check deriveFromAddress in server (routers.ts) and QuickStart result rendering.
- GAP-S: winter-sewer story — server likely computes winter-average water baseline (sewer billing basis); search server for "sewer" to find existing insight; task is client-side story card.
